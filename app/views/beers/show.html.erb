<p style="color: green"><%= notice %></p>

<%= render @beer %>

<div>
  <%= link_to "Back to beers", beers_path %> <br>
  <%= link_to "Edit this beer", edit_beer_path(@beer) if current_user %> <br>
  

  <%= button_to "Destroy this beer", @beer, method: :delete if current_user%>
</div>
